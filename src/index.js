import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
<React.StrictMode>
    <App/>
    </React.StrictMode>
);

/* <React.StrictMode>
</React.StrictMode> */ //duplán kérdezi le ezért 9-11-es sorban volt nyitó-záró tag